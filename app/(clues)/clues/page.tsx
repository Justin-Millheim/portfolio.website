import CluesApp from "./CluesApp";

// The standalone deduction puzzle. Reachable only by direct URL (/clues) —
// intentionally absent from the portfolio nav and search indexes.
export default function CluesPage() {
  return <CluesApp />;
}
